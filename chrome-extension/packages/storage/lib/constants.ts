export const INITIAL_MONKEY_POSITION = {
  x: -100,
  y: Math.floor(window.innerHeight * 0.25),
};

export const FINAL_MONKEY_POSITION = {
  x: window.innerWidth / 4,
  y: Math.floor(window.innerHeight * 0.25),
};
